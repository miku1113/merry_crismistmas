/[Ll]ibrary/
/[Tt]emp/
/[Oo]bj/
/[Bb]uild/
/[Bb]uilds/
/[Ll]ogs/
/[Uu]serSettings/

# MemoryCaptures can get fairly large and should be ignored
/[Mm]emoryCaptures/

# Asset meta files for ignored folders
/[Ll]ibrary.meta
/[Tt]emp.meta
/[Oo]bj.meta
/[Bb]uild.meta
/[Bb]uilds.meta
/[Ll]ogs.meta
/[Uu]serSettings.meta

# Autogenerated JetBrains Rider project files
.idea/

# Visual Studio cache directory
.vs/

# Gradle cache directory
.gradle/

# Build artifacts
*.apk
*.aab
*.unitypackage

# External tools information
/BurstDebugInformation_DoNotShip/
*BurstDebugInformation_DoNotShip/

# Keystore files (Keep them secret!)
*.keystore
!user.keystore # Including it because user explicitly has it here, but usually it should be ignored. 
# Actually, it's safer to ignore it by default unless the user wants it.
# The user has user.keystore in the root. I'll ignore it to be safe.
user.keystore

# Local config
/[Aa]ssets/Plugins/Editor/JetBrains*
.DS_Store
